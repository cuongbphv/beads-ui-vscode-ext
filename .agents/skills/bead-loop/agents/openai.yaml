# beads-pm-kit v0.1.0 skill:bead-loop surface:codex-yaml sha256:36b745c94d14
interface:
  display_name: "Bead Loop"
  short_description: "Work one ready Beads issue per round"
  default_prompt: "Use $bead-loop for one round; optionally add --include-partial or --dry-run."

policy:
  allow_implicit_invocation: false
